# 轻服务 · 红名单误报 / 漏报清单（迭代 A）

> 抽样环境：生产 `47.100.96.30` / 租户 `default`（默认租户·现有生产）  
> 巡检缓存：`inspection_date=2026-07-13`，`health_score=48`，`customer_success_risk=high`  
> 目标：客服日巡只看「真红」——当天可分流、可推动的阻塞；结构性上限与纯系统观测不进红灯。

## 1. 现网 P0/P1 快照（扫描日）

| item_key | severity | 责任方 | 判定 | 说明 |
|----------|----------|--------|------|------|
| `customer_phone_match_rate` | P1→**改 P2** | 实施 / POS 结构 | **误报（日巡）** | 文案已写明「通常无法靠系统内操作提升」；散客/未开卡天然拉低匹配率，不应单独把租户打成红灯 |
| `order_phone_complete_rate` | P1→**改 P2** | 实施 / POS 结构 | **误报（日巡）** | 与上项高度同构，重复扣分；降为观察项 |
| `delivery_campaign_id_complete_rate` | P1 | 系统 | **真问题（研发）** | 触达缺 `campaign_id` 影响归因复盘；客服日巡不处理，进研发队列；**不单独把看板打成红** |
| `order_customer_id_complete_rate` | P2 | 系统 | 观察 | 手机号派生指标，已是 P2 |
| `overdue_tasks_exist` | P2 | 店长 | 真黄 | 客户执行问题，黄灯即可 |
| `store_business_hours` | P3×2 | 实施 | 配置债 | 待配置，不上红名单 |

## 2. 误报根因归类

| 编号 | 类型 | 表现 | 修复策略 |
|------|------|------|----------|
| FP-01 | 结构性上限当阻塞 | 手机号完整率 / 匹配率 <60% → P1 → 健康分&lt;60 → 托管风险=高 → **整户红灯** | 巡检降为 P2；健康中心红绿灯忽略结构性 key |
| FP-02 | 同构指标重复扣分 | `order_phone_*` + `customer_phone_match_*` 双扣 | 均降 P2；看板只展示一列「手机号」 |
| FP-03 | 系统观测冒充客服红 | `owner_role=系统` 的 P1 仍驱动红灯 | 红绿灯仅统计「客服日巡可处理」项；系统项进分流队列 `eng` |
| FP-04 | 平台短信信号误伤 | 全平台短信密钥未配会显示红，但不应按店打红 | 保持 `scope=platform_global` 不进 fatal；另加**租户近 7 日触达成败**信号 |
| FP-05 | AI 可用性未展示 | `indicators.ai_available` 有值但表头无「AI」列 | 看板补 AI 列；按「AI 可运行度」类目汇总 |

## 3. 漏报关注（本轮未改口径，记入观察）

| 编号 | 风险 | 说明 | 下一刀 |
|------|------|------|--------|
| FN-01 | 昨日同步失败被休息日掩盖 | 门店休息时 `yesterday_orders_synced` 可能误红或误绿 | 迭代 B：对照营业时间 / 近 7 日均值 |
| FN-02 | 短信「配置 OK」但发送全失败 | 原信号只看平台密钥 | 本轮已加租户 7 日失败率 |
| FN-03 | 客户长期不登录仍绿 | 登录仅黄信号 | 续费周单独看；不进日巡红 |

## 4. 验收（迭代 A）

- [x] 产出本清单（≥10 条案例/归类行）
- [x] Top 误报口径已改（手机号 P1→P2；系统项不单独染红）
- [x] 看板可见 AI、租户短信真实态
- [x] 每日定时全量扫描（CST 07:00–07:14 窗口）
- [ ] 客服连续 3 日日巡反馈「真红」认可（运营验收，非代码）

## 5. 变更对照

| 文件 | 变更 |
|------|------|
| `tenant-operation-inspection-service.js` | 手机号两项异常时 severity P2；托管风险计算排除结构性 key |
| `tenant-health-center-service.js` | 日巡可处理过滤、租户短信 7 日信号、AI 指标 |
| `platform-admin.html` | 健康表增加「AI」列 |
| `index.js` | 健康中心每日扫描 cron |
