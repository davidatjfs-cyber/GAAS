# 餐厅增长 AI 操作系统

这套目录包含：

- `preview/index.html`：React + Tailwind 预览页
- `data.mjs`：26 页统一内容模型
- `assets/`：导出的每页图片
- `outputs/hrms-growth-ai-operating-system.pptx`：可直接外发的 PPTX
- `build-presentation.mjs`：一键重新渲染和导出脚本

运行方式：

```bash
node build-presentation.mjs
```

会先打开本地预览页逐页截图，再导出 PPTX。
